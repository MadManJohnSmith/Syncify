[package]
name = "syncify-lyrics-domain"
version = "0.1.0"
edition = "2021"
description = "Syncify Lyrics Domain Contract & Pure Deterministic Engine"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1.10"
